{
  "name": "@us/bili-quickcopy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@us/tools": "workspace:*",
    "@us/shared": "workspace:*"
  }
}